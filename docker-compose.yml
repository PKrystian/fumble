services:
  dev:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host"
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules

  web:
    build:
      context: .
      target: runtime
    ports:
      - '8080:80'
